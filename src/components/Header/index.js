import React from 'react';

function Header () {
    return (
        <div className="page-header d-flex justify-content-center">
            <h1 className="title-header">Abdalla's Portfolio</h1>
        </div>
    );
};

export default Header;